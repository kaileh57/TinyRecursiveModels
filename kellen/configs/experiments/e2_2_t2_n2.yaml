# Experiment E2.2: T/n Schedule - Shallow (T=2, n=2)
# Test with minimal recursion depth

defaults:
  - ../base_sudoku_tpu

model:
  H_cycles: 2  # Reduced from 3
  L_cycles: 2  # Reduced from 6

checkpoint:
  save_dir: "checkpoints/e2_2_t2_n2"

logging:
  tensorboard_dir: "gs://your-bucket-name/logs/e2_2_t2_n2"

# Expected: Faster training but lower accuracy
